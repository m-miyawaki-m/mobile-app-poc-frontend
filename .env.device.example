# 実機検証用テンプレート
# このファイルをコピーして .env.device を作成し、<PCのLAN_IP> を実値に置換すること。
# PCのIP確認: PowerShell で `ipconfig | Select-String "IPv4"`
# mock も `npm run mock:lan` で 0.0.0.0 起動が必要。

VITE_API_BASE=http://<PCのLAN_IP>:4010
VITE_LOGIN_USERNAME=demo-user
VITE_LOGIN_PASSWORD=demo-password
